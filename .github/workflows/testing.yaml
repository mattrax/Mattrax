name: Testing

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  merge_group:

jobs:
  testing:
    name: Testing
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - using: "docker"
        image: "ghcr.io/microsoftgraph/msgraph-cli:latest"

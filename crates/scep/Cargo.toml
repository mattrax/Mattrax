[package]
name = "scep"
version = "0.0.0"
edition = "2021"
publish = false

[lints]
workspace = true

[dependencies]
bcder = "0.7.4"
bytes = "1.9.0"
cryptographic-message-syntax = { workspace = true, default-features = false }

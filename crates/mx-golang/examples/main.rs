// use mx_golang::PrintConsole;

use mx_golang::scep_success;

fn main() {
    scep_success();
    // unsafe {
    //     let s = "Hello, World from Rust via Go!";
    //     let c = std::ffi::CString::new(s).unwrap();
    //     PrintConsole(c.as_ptr() as *mut i8);
    // }
}

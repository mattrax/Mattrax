Future:
 - Codesign the enrollment profile on macOS. This will be problematic with Lambda as we don't have access to the HTTPS certificates (can we issue our own for this usecase?).

Features:
 - Custom SCEP certificates. Eg. WiFi/VPN crendentials which Mattrax handles issuing.
